<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>LivingTech Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white min-h-screen">
  <div class="min-h-screen flex">
    <main class="flex-1 p-6 space-y-6">
      <header class="flex justify-between items-center">
        <h2 id="user-email" class="text-2xl font-semibold">Welcome</h2>
        <button onclick="logout()" class="bg-white text-black px-4 py-2 rounded">Logout</button>
      </header>

      <section class="grid md:grid-cols-3 gap-6">
        <div class="bg-gray-800 rounded-2xl p-6">
          <h3 class="text-xl font-bold mb-2">Coin Balance</h3>
          <p id="coin-balance" class="text-3xl font-mono">0</p>
        </div>
        <div class="bg-gray-800 rounded-2xl p-6">
          <h3 class="text-xl font-bold mb-2">Daily Reward</h3>
          <button onclick="claimDailyReward()" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded mt-2">Claim 50 Coins</button>
        </div>
      </section>

      <section class="grid md:grid-cols-2 gap-6">
        <div class="bg-gray-800 rounded-2xl p-6">
          <h3 class="text-xl font-bold mb-4">Add Social Media Handle</h3>
          <form onsubmit="addHandle(event)" class="space-y-4">
            <input id="handle-input" type="text" placeholder="@yourhandle" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white">
            <select id="handle-platform" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white">
              <option>Instagram</option>
              <option>TikTok</option>
              <option>X (Twitter)</option>
              <option>YouTube</option>
              <option>Facebook</option>
            </select>
            <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded w-full">Add Handle</button>
          </form>
          <div class="mt-6">
            <h4 class="text-lg font-semibold mb-2">Your Handles</h4>
            <ul id="user-handles" class="space-y-1 text-sm text-gray-300"></ul>
          </div>
        </div>

        <div class="bg-gray-800 rounded-2xl p-6">
          <h3 class="text-xl font-bold mb-4">Add Follow Task</h3>
          <form onsubmit="addTask(event)" class="space-y-4">
            <input id="task-handle" type="text" placeholder="@targethandle" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white">
            <select id="task-platform" class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white">
              <option>Instagram</option>
              <option>TikTok</option>
              <option>X (Twitter)</option>
              <option>YouTube</option>
              <option>Facebook</option>
            </select>
            <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded w-full">Add Follow Task (20 coins)</button>
          </form>
        </div>
      </section>

      <section>
        <div class="bg-gray-800 rounded-2xl p-6">
          <h3 class="text-xl font-bold mb-4">Follow to Earn Coins</h3>
          <ul id="tasks-list" class="space-y-2"></ul>
        </div>
      </section>
    </main>
  </div>
  <script src="frontend.js"></script>
</body>
</html>
